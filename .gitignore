# Secrets / env
.env

# Shell / user dotfiles
.bash_profile
.bashrc
.profile
.zprofile
.zshrc
.gitconfig
.ripgreprc

# Git internals (only when present as untracked artifacts)
.gitmodules

# Editors / IDEs
.idea
.vscode

# Python build / cache artefacts
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Agent / AI tooling
.agents/
.claude/
.clinerules
.kilocode/
.mcp.json
MEMORY.md

# Pseudonymize artefacts (HMAC key, mapping, report) — never commit
.key
*.key
pseudonymize-mapping.json
pseudonymize-report.jsonl
