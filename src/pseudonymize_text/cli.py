"""CLI entry point. Implementation pending — see docs/USAGE.md for the planned contract."""

import sys


def main() -> int:
    """Stub CLI entry. Prints a not-implemented notice and exits 0."""
    print(
        "pseudonymize: not yet implemented (v0.0.1 is a scaffold). See docs/USAGE.md.",
        file=sys.stderr,
    )
    return 0


if __name__ == "__main__":
    sys.exit(main())
