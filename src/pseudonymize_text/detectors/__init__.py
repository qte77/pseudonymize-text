"""Detector adapters that emit ``replacer.Span`` instances.

Sub-modules: ``terms`` (literal + pattern from a term list), ``structured``
(email / phone / IBAN / CC / SSN), ``ner`` (optional spaCy adapter).
"""
